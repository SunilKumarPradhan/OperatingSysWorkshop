#include <stdio.h>

int main() {
    int a, b, s;

    // Read values for 'a' and 'b', and store the second input in 'a'
    s = scanf("%d%d%d", &a, &b, &a);

    // Print the sum of 's' and the result of the printf statement
    printf("%d\n", s + printf("OSW CSE="));

    return 0;
}
